import { FC } from 'react';
import { SettingsMenuEnum } from '.';
interface RegionMenuProps {
    removeMenuPopup: (menu: SettingsMenuEnum) => void;
}
export declare const RegionMenu: FC<RegionMenuProps>;
export {};
